name: Prepare for bloomxq

on:
#  push:
#    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-bloomxq:
    uses: ./.github/workflows/build.yml
    with:
      device: bloomxq
      device_name: Galaxy Z Flip 5G
    secrets:
      TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
      TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
